def suma(numero1, numero2): #declaro la funcion pero con parametros
	print(numero1+numero2)

suma(4, 7)  #se almacenan los valores en su lugar y se ejecuta el codigo
suma(2, 4)  #los parametros pueden ser texto y numeros a la vez sin drama
